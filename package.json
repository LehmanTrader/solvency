{
  "name": "solvency",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cost per solved task for AI coding models. Every number carries a source and a last_verified date.",
  "license": "CC-BY-4.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "coverage": "node scripts/coverage.ts",
    "table": "node scripts/table.ts",
    "charts": "node scripts/charts.ts",
    "report": "npm run charts && npm test",
    "charts:light": "DENOM_THEME=light node scripts/charts.ts",
    "pdf": "npm run charts && npm run charts:light && node scripts/render-pdf.ts reports/2026-08-cost-per-solved-task.md",
    "pdf:note2": "npm run charts && npm run charts:light && node scripts/render-pdf.ts reports/2026-08-same-model-four-harnesses.md",
    "pdf:note3": "npm run charts && npm run charts:light && node scripts/render-pdf.ts reports/2026-08-what-is-a-task.md",
    "pdf:all": "npm run pdf && npm run pdf:note2 && npm run pdf:note3",
    "png": "node scripts/png.ts",
    "og:cards": "node scripts/og-cards.ts",
    "budget": "node scripts/bench-budget.ts",
    "docx": "node scripts/docx.ts",
    "watch": "node scripts/watch-prices.ts",
    "queue:miner": "node scripts/content-miner.ts",
    "queue:price-watch": "node scripts/price-watch-draft.ts",
    "queue:dashboard": "node scripts/queue-dashboard.ts",
    "site": "cd site && npm run build",
    "bench": "node bench/runner.mjs",
    "bench:selftest": "node bench/runner.mjs --selftest",
    "bench:gui": "node bench/server.mjs"
  }
}
