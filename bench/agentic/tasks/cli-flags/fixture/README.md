# wordcount

`node bin/wordcount.js --file notes.txt` prints the word count (whitespace-separated tokens).
With `--lines`, prints the line count (trailing newline does not create an extra line).
Flags combine in any order. Output is the bare number plus a newline.
