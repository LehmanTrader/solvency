{
  "id": "semver",
  "func": "semverCompare",
  "title": "Semver compare",
  "timeoutMs": 8000,
  "prompt": "Write `semverCompare(a, b)`: compare two dotted numeric version strings (e.g. '1.2.10' vs '1.3'); missing parts count as 0. Return -1, 0 or 1. Reply with exactly one JavaScript code block (```js ... ```) that uses `export function` for the required function. ES module syntax, no external imports, no I/O, no comments needed."
}